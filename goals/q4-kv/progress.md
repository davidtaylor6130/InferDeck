# Q4 KV progress

## 2026-09-23/24 checkpoint

Status: IN_PROGRESS. Current production alpha55 BF16/R4D is running; after a guarded diagnostic interruption and service restart, `qwen3.8-27b-radiance` was loaded and a real `/v1/chat/completions` request returned HTTP 200 with `OK` (14 generated tokens). No Q4 deployment.

The private decode overlay has synthetic GPU parity, but no real InferDeck request has exercised the new decode DLL. A later optional overlay probe passed parity with a malformed PowerShell context argument (`64,65536` became `6465536`), so it is **not** the intended 64K check. The user directed us to stop offline checks and test through the actual app. The probe and watchdog were stopped. The probe's restore code used an unsupported `Expect: 100-continue` request and GPU admission was 4 MB short after unloading; an authorised service restart restored production. Evidence: `.UAM/AINotes/int4-decode-window-20260923T234011229Z.json`, `.UAM/AINotes/restore-inferdeck-service-result.json`.

Tracked source changes are the opt-in INT4 decode DLL selection/contract and focused tests. Private overlay and HIP kernel are under `build/runtime-radiance-probe/r4d-feasibility/int4-fast/`. Candidate stage is `build/private-releases/alpha59-int4decode/`; its copied alpha58 manifest is not yet a test result. Branch `agent/runtime-pp-context-evidence` has five modified tracked files. No commit this checkpoint.

Alpha59 staging is complete: Release gateway built; Python profile tests 27/27 and focused C++ model/config suites 2/2 passed. The staged executable and four bridge modules match the built/source files; config and manifest pin the decode DLL SHA-256. Stage manifest remains `STAGED_NOT_REAL_TESTED` until a real app run. The isolated test recovery scripts now use the documented swap endpoint without an unsupported request body; PowerShell syntax validation passed.

Next exact action: run `.UAM/AINotes/int4fast-smoke.ps1 -StageName alpha59-int4decode -LongContext -ServiceStop` once through the staged InferDeck app. Check gateway logs, actual greeting, frozen two-agent worker, production restoration, and whole-loop comparison. If startup or greeting fails, fix that specific failure before repeating a long run.

## 2026-09-24 alpha59 real-app result

Alpha59 ran through the actual staged Windows InferDeck gateway on port 11435, with production service stopped and independently recoverable. Real Qwen3.8 27B loaded with INT4 KV, native `r4d_int4` prefill and decode DLL active, context 106496, and one coherent greeting in 2.691 s. Frozen two-agent 64K+2K tool loop passed both exact tasks, four HTTP 200 requests, and 63840 cached tokens per continuation. Complete client batch time was 200.737 s. The current BF16/R4D three-run median on that fixture is 133.684 s, so this Q4 candidate is 50.16% slower and fails the 147.052 s initial similar-speed screen. Alpha58 prefill-only INT4 took 184.150 s in one run, but output-token counts differ, so that difference alone does not isolate the decode kernel. Raw: `.UAM/AINotes/alpha59-int4decode-smoke-20260923T235103217Z.json` and `.UAM/AINotes/q4kv-64k2k-20260923T235155609Z-worker.json`. Stage manifest is `FAIL_TOTAL_TIME_DO_NOT_DEPLOY`.

Production alpha55 BF16/R4D was restored, with the original model loaded, the staged port closed, and a real chat reply `OK` / HTTP 200. Recovery took about two minutes because service startup loaded its configured default Ornith model before swapping to Qwen. No Q4 production deployment. The goal is not complete.

Exact alpha59 request statistics are in `build/private-releases/alpha58-int4fast/data/stats.db` rows 23-26 because the copied alpha59 YAML still pointed its log/state/store/stats paths into alpha58. The worker IDs identify those rows; do not confuse them with alpha58 rows 18-21. Alpha59 queue was 0/54.3/54.4/66.4 s, cold prompt evaluation 44.688/44.797 s at 1435/1432 PP, cached append evaluation 2.047/2.046 s for 2333 uncached tokens at ~1140 PP, and generation 7.57-7.90 TPS. Q4 decode is slower than alpha58's 9.76-10.07 TPS. The staged config paths are now isolated to alpha59 and parse as YAML; current config SHA-256 `edccfc981602800e3dfbd50837fe05524083f31f0169b89809b58e3f5e14f066`, which differs from the measured run. No new performance claim applies to the corrected config. The 30000 MB residency figure is an estimate; peak dedicated VRAM was not captured.

Next exact action: inspect the private Q4 decode HIP kernel and the measured 7.57-7.90 TPS path for one bounded speed change. Then rebuild/stage and rerun the same frozen real InferDeck fixture once. Retain production BF16 until Q4 passes the total-time gate.

## 2026-09-24 alpha64 four-slot checkpoint

Status: IN_PROGRESS. The goal tool now tracks the four-slot Q4 objective. Production `0.9.0-alpha-55` BF16/R4D was restored after the guarded stage and is serving its original `qwen3.6-35b-a3b` model. Do not describe Q4 as deployed.

Alpha61-63 four-request tests failed three of four requests with a vLLM `KeyError` at `_gather_mm_embeddings`. Their stage roots had an updated Python profile, but the gateway actually imported stale `stage/python/inferdeck_vllm_radiance_profile.py`; those runs did not test the new engine lock. Alpha64 copied and hash-checked the imported path against tracked source before interrupting production. Real Qwen3.8 Q4 loaded with 169 KV blocks, group-aware calculated maximum concurrency 4.0238 at context 106496, and native INT4 prefill/decode. Four simultaneous distinct short API requests returned HTTP 200 with four active slots at peak. The post-request GPU-wide VRAM-used telemetry sample was 28454 MiB, not a process allocation or peak. Evidence: `.UAM/AINotes/alpha64-int4loaded-smoke-20260924T005613404Z.json`, its `.four-slots.json`, staged stderr, and `build/private-releases/alpha64-int4loaded/candidate-manifest.json`.

The frozen two-agent 64K+2K tool loop produced four HTTP 200 responses; both exact BUILD_TAG tasks passed and each warm continuation reported 63840 cached tokens. Batch wall time was 151.471 s versus BF16/R4D three-run median 133.684 s, 13.3% slower and above the initial 147.052 s screen. The Q4 run generated 894 tokens total versus 1183 for the BF16 baseline, so output-length differences do not explain away the slowdown. Q4 cold prompt evaluation was ~45.985/46.062 s at 1395/1392 PP; warm append evaluation ~2.078/2.093 s at ~1122 PP. One Q4 cold request had 90.2 s visible TTFT despite ~46 s engine prompt duration. Gateway queue time was <0.2 ms per request; source timing does not yet distinguish vLLM scheduling delay from bridge/parser delivery. Raw: `.UAM/AINotes/q4kv-64k2k-20260924T005720082Z-worker.json` and `build/private-releases/alpha64-int4loaded/data/stats.db`.

The outer worker incorrectly marked the run FAIL because it required exactly 66173 warm prompt tokens. One valid tool call changed the warm prompt to 66188 tokens; the other remained 66173. Its exact answer/quality validator passed. The ignored worker now checks a 2000-4000-token uncached append, >=63000 cached tokens, and output headroom; `node --check` and synthetic fixture preflight passed. The raw alpha64 record is unchanged and remains marked FAIL; repeat real measurement is needed for a PASS under the corrected worker. The stage script's generic 'quality, cache, or fixture' failure text was overly broad. Production recovery was independently checked: health true, original model loaded, no active swap.

Current tracked edits are config/model contract tests, four-slot C++ lifecycle/GIL/output handling, Python INT4 profile/engine lock and tests, plus this goal record. The native HIP source/overlay and DLLs are still in ignored `build/`; a Luna agent is copying minimal source to tracked wrapper files without touching existing source. No commit yet. Image support is optional for Q4; the existing Vulkan vision route must remain functional.

Next executable action: inspect the source-copy result and preserve/build the exact Q4 overlay and HIP kernels, then make one measured change to improve the 151.471 s two-agent loop or document why BF16 remains the speed choice. Re-stage with imported-profile/overlay hash preflight, rerun the corrected real-app worker, and verify production recovery. After a competitive result, exercise four long ~100K requests and commit only tested functional source.

## 2026-09-24 source-layout retry checkpoint

Minimal INT4 HIP prefill/decode source and the exact overlay bytes from alpha64 were copied under `libs/vllm_radiance_wrapper/native/int4/`. Both HIP translation units compiled with ROCm 7.1 for gfx1201; Windows host DLLs expose the required prefill and decode C exports. The copied overlay has the same SHA-256 as alpha64. The pinned vLLM/Radiance/Python/ROCm dependencies remain external. `native/int4/README.md` records the commands and unresolved upstream source-license provenance; no push/publication is approved.

Alpha65 was the first stage built from those tracked source files. It failed during model load because the byte-identical overlay was placed under `python/`, while its own source-hash check resolves a sibling `kernel/` directory. The guarded runner stopped only the failed staged gateway and restored production alpha55 with its original `qwen3.6-35b-a3b` model. Evidence: `.UAM/AINotes/alpha65-int4tracked-smoke-20260924T010958279Z.json` and its staged stderr. This is a source-layout failure, not a model/kernel correctness result.

The overlay was moved without changing bytes to `native/int4/r4d-feasibility/int4-fast/overlay/`, beside the expected `kernel/`. Alpha66 has isolated data/config, reuses the freshly compiled source DLLs, and pins the actual imported profile, overlay, executable, YAML and DLL hashes. The stage preflight now checks the overlay's source-relative kernel paths before production interruption. A guarded alpha66 real-app run with four short requests and the corrected 64K+2K worker is in progress. The Python profile also records one concise Q4 engine queue/prefill/generation timing event per completed request to attribute the prior 44-second TTFT gap. This diagnostic passed 31 focused Python tests after a mock-state compatibility fix.

Next executable action: inspect alpha66 run record and stderr timing events, verify original-model recovery, then decide whether one bounded tuning change is justified. Do not treat alpha65's failed load or alpha64's outer worker FAIL as passing evidence. A separate four near-100K cold-request worker was prepared but has not been run.

## 2026-09-24 alpha66 measured result

Alpha66 source-rebuilt stage loaded real Qwen3.8 with INT4 KV, native prefill/decode DLLs, four 106496-token slots, and the relocated tracked overlay. Four distinct simultaneous short requests all returned HTTP 200 and peak active slots were four. The corrected frozen two-agent 64K+2K worker passed both exact tool tasks, all four HTTP requests, and 63840 cached tokens on each append. Complete batch time was 151.354 s, versus BF16/R4D median 133.684 s and the 147.052 s initial screen. It is 13.22% slower than BF16 in this fixture; do not deploy/promote for speed. GPU-wide VRAM-used samples were about 28450 MiB after four short requests and 29502 MiB after the long loop; neither is a process allocation or inference peak. Production alpha55 and its original `qwen3.6-35b-a3b` model restored; staged process exited. Evidence: `.UAM/AINotes/alpha66-int4layout-smoke-20260924T011354761Z.json`, `.four-slots.json`, `.gateway.stderr.log`, `.UAM/AINotes/q4kv-64k2k-20260924T011502948Z-worker.json`, stage `data/stats.db`, and `candidate-manifest.json`.

The new per-request engine timing records show 43.937 s internal vLLM queue before the second cold 64131-token request, 45.922/46.031 s prompt evaluation on the two cold requests, and a 57.265 s generation span for the first while it was interleaved with the second prefill. InferDeck gateway queue was effectively zero. Thus the earlier ~44-second TTFT gap is vLLM internal scheduling, not proven bridge starvation. Alpha64 and alpha66 wall times differed by only 0.117 s, with the same 894 total generated tokens; the copied source/build did not improve speed. All 148 C++ unit/integration CTest cases and 31 Python profile tests passed; the Windows HIP source built with required exports. No public push due unresolved source-license provenance.

Next executable action: make a local tested experimental checkpoint commit, then try `long_prefill_token_threshold=2048` for INT4 only while retaining batch 4096 and all model/context/sampling settings. Run the same real two-agent fixture once, record internal queue, total time, output lengths and quality, and keep/revert based on whole-loop time. If the candidate is competitive, run four actual near-100K requests; otherwise still test capacity on the best functional profile before claiming four-slot parity.

## 2026-09-24 alpha67 prefill-threshold screen

Local experimental source checkpoint `71b8f21` preserved alpha66; no push or Q4 production deployment. Alpha67 changed only the INT4 vLLM `long_prefill_token_threshold` to 2048, leaving the 4096 batch, four 106496-token slots, model, quantization, native DLLs, sampling and fixture unchanged. The actual imported Python profile matched tracked source and the staged manifest SHA-256 before the guarded real-app run. The model loaded coherently, four distinct short requests passed with four active slots, and the frozen two-agent 64K+2K tool fixture passed both exact tasks, all four HTTP responses, and 63840 cached tokens per warm append. Complete batch time was 137.930 s versus alpha66 151.354 s and BF16/R4D three-run median 133.684 s. This is one screen, not a performance acceptance result. Alpha67 generated 667 output tokens versus alpha66's 894, so the wall-time difference cannot be attributed solely to the scheduler setting. Internal queue fell from 43.937 s for alpha66's second cold request to zero for both alpha67 cold requests, while per-request prefill grew to 87.375/88.000 s versus about 46 s. Production alpha55 and its original model restored, and the staged process exited. Evidence: `.UAM/AINotes/alpha67-prefill2048-smoke-20260924T012715261Z.json`, its `.four-slots.json` and `.gateway.stderr.log`, `.UAM/AINotes/q4kv-64k2k-20260924T012822657Z-worker.json`, and `build/private-releases/alpha67-prefill2048/candidate-manifest.json`.

Next executable action: finish the second guarded alpha67 real-app stage already in progress. It repeats the frozen two-agent fixture and then sends four independent near-100K cold requests with 256-token output headroom and requires four observed active slots, HTTP 200, no prefix hits or truncation. Compare both full-loop samples, validate the four-context capacity, verify production recovery, then keep or revert the threshold and make a tested local commit if kept. Radiance Q4 advertises text only; the separate Vulkan Qwen profile still has `has_vision: true` and its configured mmproj.

## 2026-09-24 alpha67 confirmation and four-100K probe repair

The second unchanged alpha67 real-app two-agent 64K+2K tool loop passed in 136.871 s, again with both exact tasks and 63840 cached tokens on each continuation. The two alpha67 samples were 137.930/136.871 s, both under the original 147.052 s similar-speed screen and about 2.8% slower than the BF16/R4D 133.684 s median. Their generated-token counts matched each other (667), but were below alpha66's 894; this remains a task-level latency result, not proof that the threshold itself increased compute throughput. Evidence: `.UAM/AINotes/alpha67-prefill2048-smoke-20260924T013335202Z.json`, `.UAM/AINotes/q4kv-64k2k-20260924T013439553Z-worker.json`, and that stage's stderr log.

Four simultaneous near-100K requests were admitted, and polling observed four active slots, but the first capacity worker failed all four with `TypeError: fetch failed` at 304.28 s. The candidate gateway was still serving status HTTP 200 immediately before the client failures; the guarded runner stopped it after the failed assertions and restored production alpha55 with its original model. There are no completed 100K responses and no capacity PASS. The synchronized ~300 s cutoff points to the worker's HTTP transport deadline; it is not evidence of a model crash. The ignored capacity worker now uses `node:http` with an explicit 720 s abort signal, polls status every 250 ms, and puts a unique lane identifier at the start of each system prompt to prevent shared prefix hits. Model/profile/configuration are unchanged. Raw failed evidence: `.UAM/AINotes/alpha67-prefill2048-smoke-20260924T013335202Z.four-100k.json` and the corresponding staged `logs/gateway.log`.

Next executable action: syntax-check the changed capacity worker, confirm production idle and alpha67 imported-profile hashes, then rerun only `-FourLong` under `.UAM/AINotes/int4fast-smoke.ps1 -StageName alpha67-prefill2048 -ServiceStop`. Capture four HTTP responses, prompt/cached counts, finish reasons, VRAM and production recovery. If it still fails, diagnose the earliest actual server or client error rather than assuming capacity or a GPU crash.

## 2026-09-24 four-100K input capacity result

The targeted alpha67 retry used `node:http` with a 720 s client timeout and four independent lane prefixes. All four real InferDeck requests returned HTTP 200; each reported 100194 input tokens and zero cached tokens, with four active slots observed. They completed in 347.254-363.424 s, and the complete concurrent batch took 363.641 s. A `windows_pdh_dxgi` GPU-wide VRAM-used sample after the batch was 29557.656 MiB on the 32021 MiB R9700; it is not process-specific or a peak. Two replies ended normally at 186/192 output tokens; two reached the deliberately requested `max_tokens=256` cap. The original strict capacity worker and stage therefore remain `FAIL` on their complete-answer assertion. The separate executed audit `.UAM/AINotes/alpha67-prefill2048-four100k-capacity-audit.json` passed the narrower four-100K **input-capacity** checks, including HTTP status, input allowance, cold cache, four active slots, explained output limits and production recovery. It does not pass long-context answer quality or a no-output-cap test. Raw: `.UAM/AINotes/alpha67-prefill2048-smoke-20260924T014547546Z.four-100k.json`, `.UAM/AINotes/alpha67-prefill2048-smoke-20260924T014547546Z.json`, staged stderr/gateway log. Production alpha55 with original `qwen3.6-35b-a3b` was restored and port 11435 closed.

The ignored future capacity worker now treats an explicit output cap as an explained finish rather than silently conflating it with context truncation. The runner's null worker-exit-code check was also corrected to match its existing long-context worker handling. These are harness-only edits made after the raw run; the recorded alpha67 model/profile/binary and raw evidence remain unchanged. No more GPU runs are needed for the scoped input-capacity finding.

Next executable action: keep the tested 2048-token INT4 threshold and make a local commit after reviewing the final profile/goal diff and evidence. Then prepare an opt-in production configuration and rollback instructions while leaving BF16 as the running default. Before calling the full Q4 goal complete, verify 100K answer quality with enough output budget, repeated switching/mixed-request stability, and the actual deployment path; unresolved native-source publication rights preclude a public push.

## 2026-09-24 focused 100K tool-quality result

The unchanged alpha67 staged InferDeck gateway passed one frozen 100K tool-continuation case through its real HTTP API. The private source fixture SHA-256 was `94ae060e5209d2204e95b1856fb9e4029030957268842de798834b68b5e5fab6`; only its model ID was changed in an ignored working copy. The first HTTP 200 request contained 100417 prompt tokens, made the exact `read_file({"path":"main.cpp"})` tool call, and finished with `tool_calls`. After the approximately 4K tool result, the second HTTP 200 request contained 104560 prompt tokens, reported 100320 cached tokens, produced the required `BUILD_TAG_7C93A1` fact, and finished with `stop`. The shared exact tool-quality validator passed. Client latencies were 86.324 and 12.746 s, for 99.074 s from first send to completed continuation. This proves one uncapped 100K answer/tool continuation, not four simultaneous complete 100K answers or long-term quality. Evidence: `.UAM/AINotes/alpha67-prefill2048-smoke-20260924T020225436Z.json`, its `.quality100k.result.json` and `.quality100k.validation.txt`, and the original fixture under `build/runtime-radiance-probe/real-model-evidence/`.

The guarded runner closed the staged gateway and restored the original production `0.9.0-alpha-55` BF16 service with `qwen3.6-35b-a3b` loaded, no swap in progress, zero active requests, and a healthy endpoint. No Q4 production deployment occurred. The prior local source checkpoint is `413153e`. Radiance Q4 currently advertises text only; retain the separate Vulkan Qwen3.8 model with mmproj/vision. The owner accepts Radiance vision as optional and does not want it to delay Q4.

Next executable action: review the isolated candidate and current live configuration for a minimal opt-in deployment that keeps BF16 default, existing aliases, and Vulkan vision intact. Prepare exact config/artifact paths and rollback before changing production; then run the smallest necessary live validation and record actual service recovery. Repeated switch/mixed-request reliability and four simultaneous complete 100K answers are still unproven. Do not claim the whole Q4 goal complete or push the native source publicly while provenance is unresolved.

## 2026-09-24 alpha68 active-profile opt-in package

The committed alpha67 code was rebuilt locally as `0.9.0-alpha-68 revision=2332e133f82a28c3b016afa16cc919e89e378f0c dirty=false`; the executable SHA-256 is `77c8bab84495e82d70ac672383c0723439df602984b190f703e9b1eb131befd1`. Build succeeded and all 148 C++ unit/integration CTests passed on that build. The alpha68 binary has not served a real model yet; alpha67 real-app latency/quality results do not automatically become alpha68 hardware evidence.

Read-only production inspection found `GET /api/inferdeck/v1/config` reports `usingActiveProfile: true`. `C:\InferDeck\config\gateway.active.yml` differs substantially from base `gateway.yml` (including default and aliases), so the first base-file opt-in draft was invalidated. The corrected candidate appends a new `qwen3.8-27b-radiance-q4` stanza inside the **active** profile while preserving all existing bytes and parsed values. It keeps the existing one-slot BF16 Radiance ID, active default/aliases, and Vulkan Qwen3.8 mmproj/vision. The candidate active YAML SHA-256 is `f41e94924b2b56bd02d72fd0224fb8bb2aa16bd7595e0e40fac4c731f6ba68f5`. The ignored reviewable package is `build/private-releases/alpha68-q4-active-optin/`; its manifest status is `PREPARED_NOT_DEPLOYED_NOT_HARDWARE_TESTED`. The candidate matches all 24 live root DLLs and static assets by SHA-256, so activation need only replace the executable and embedded Python profile, add the two Q4 DLLs/overlay, and install the reviewed active YAML. Details and rollback are in `goals/q4-kv/opt-in-deployment.md`.

Production remains the healthy original alpha55 service with `qwen3.6-35b-a3b` loaded. The Q4 package still relies on nine existing external runtime/model locations under the checkout's ignored build tree, just as the current BF16 Radiance profile does; do not clean that tree. The native INT4 source's public distribution rights remain unresolved. No Q4 production install or alpha68 real-model test has occurred.

Next executable action: finish and run the deployment helper's **read-only preflight**, review its backup/watchdog/rollback logic and package hashes, then activate the opt-in candidate under the production authorization boundary. Validate one real Q4 request, the existing BF16/Vulkan inventory and restoration of the original model; rollback on failure. Keep the original broad reliability gates open unless actually executed.

The deployment helper passed read-only preflight as `PREFLIGHT_OK alpha68-q4-optin-20260924T022938735Z`; evidence is `.UAM/AINotes/alpha68-q4-optin-20260924T022938735Z.json`. It verified the live NSSM target, active-profile use, idle service, current executable/active/base hashes, all ten package file hashes, 24 matching root DLLs, unchanged static assets, and existing BF16 Radiance registry ID. Health and hashes afterward confirmed production still runs alpha55 with `qwen3.6-35b-a3b`; no live install. An independent adversarial review found five concrete deploy/rollback faults, all fixed in the ignored helper and parser-checked before the successful preflight; see `opt-in-deployment.md`. The initial preflight failed because the package omitted root DLLs needed even for `--version`; the helper now verifies live DLL hashes first and temporarily uses their directory on `PATH` only for that provenance check. No production change occurred during either preflight.

Next executable action: obtain the required explicit authorization to replace the live executable and active YAML. Then run `.UAM/AINotes/Deploy-Alpha68Q4OptIn.ps1 -Execute -RootReviewed`, inspect its backup/watchdog evidence, verify Q4 live chat and restoration of the original model, and update acceptance status based on actual results. If activation fails, keep the rollback record and diagnose that failure; do not label alpha68 production-ready solely from preflight. The original 20-switch/100-mixed-request and four complete 100K answer gates remain unrun.

## 2026-09-24 evidence provenance and final-artifact gap

Read-only audit confirmed four completed guarded alpha67 stage/production recovery cycles. The alpha67 stage stats DB contains 34 Q4 requests recorded as HTTP 200, including 16 repeats of the four-short fixture. There is no evidenced cancellation test, 20-cycle switch test or 100 mixed-request run. The two known four-100K probes include one synchronized client transport failure and one run in which two answers reached the explicit 256-token output cap; neither is a full answer-quality PASS. These raw failures remain in place. Evidence: the four alpha67 `smoke-*.json` records listed in `acceptance.json`, the stage `data/stats.db`, and their worker artifacts.

The `29557.65625` MiB alpha67 post-four-100K value came from `windows_pdh_dxgi` as GPU-wide VRAM used at timestamp `1790214778844`, not process-dedicated allocation or peak inference usage. Earlier 28454/29502 MiB figures have the same GPU-wide provider. The Q4 dedicated-VRAM parity claim is therefore unverified. The closest saved Vulkan Qwen3.8 two-agent 64K+2K wall time is 264.554 s in `build/perf/matched-runtime-baseline/r73-vulkan-quality64k2-20260921.summary.json`, but it uses a different fixture SHA than the Radiance Q4 worker, so no direct same-workload Vulkan speedup or memory-parity ratio is supported. Keep BF16/R4D 133.684 s as the matched two-agent loop comparison used for the Q4 speed screen.

Next executable action while production replacement approval is pending: run one guarded real InferDeck alpha68 isolated smoke on port 11435 with the final new `qwen3.8-27b-radiance-q4` ID, coherent greeting and four short concurrent requests; verify alpha55 original-model recovery. Record the exact alpha68 binary/config/profile/native hashes and do not promote the alpha67 hardware results as alpha68 measurements. Then address the remaining memory/reliability gates with matched, correctly scoped evidence rather than an inferred comparison.

## 2026-09-24 alpha68 final-binary isolated smoke

The first guarded alpha68 run failed during model load because the staged and opt-in packages omitted `kernel/r4d_int4_tiled_prefill.hip` and `kernel/r4d_int4_tiled.h`, which the INT4 overlay reads for source-hash diagnostics. The runner restored the original alpha55 service and `qwen3.6-35b-a3b` model. Raw failure: `.UAM/AINotes/alpha68-q4-smoke-smoke-20260924T024026738Z.json` and staged `logs/gateway.log`. Both source files now match tracked native source SHA-256 in the stage and opt-in package; manifests, deployment targets, rollback allowlist and stage preflight include them. The control POST helpers also suppress the gateway-rejected `Expect` header. PowerShell/Python syntax checks and source/package hash checks passed. The corrected package's read-only preflight passed as `.UAM/AINotes/alpha68-q4-optin-20260924T024835498Z.json`.

The corrected final alpha68 executable (`2332e133f82a28c3b016afa16cc919e89e378f0c`, SHA-256 `77c8bab84495e82d70ac672383c0723439df602984b190f703e9b1eb131befd1`) loaded real Qwen3.8 Q4 with INT4 KV and four configured 106496-token slots in isolated InferDeck on port 11435. A coherent greeting returned in 3.393 s. Four simultaneous distinct short requests returned HTTP 200 with expected words and peak active slots of four; the batch lasted 4.788 s. This is a short smoke only: it does not establish long-context speed, output quality at 100K, dedicated VRAM parity or 20-cycle/100-request reliability on alpha68. The `windows_pdh_dxgi` 28449.633 MiB post-short-request value is GPU-wide VRAM usage, not process-dedicated or peak. The guarded runner exited `PASS_SHORT_SMOKE_ONLY`, restored the original alpha55 service/model, and closed port 11435. Evidence: `.UAM/AINotes/alpha68-q4-smoke-smoke-20260924T024851035Z.json` and its `.four-slots.json`; the stage/package manifests contain source, executable, config and native artifact hashes. No production deployment occurred.

Next executable action: verify the corrected opt-in package's read-only preflight after the manifest status update, then run one frozen alpha68 two-agent 64K+2K tool-loop if the worker can target the new model ID without changing its fixture or acceptance checks. Compare complete batch time to BF16/R4D 133.684 s and alpha67 136.871/137.930 s; restore production and keep the result scoped. Production executable/active-config activation is still awaiting explicit authorization. Keep 100K four-answer, dedicated-VRAM and broad reliability gates open until actually measured.

## 2026-09-24 final alpha68 two-agent task

The unchanged frozen fixture SHA-256 `d4b52041f8e99d3e486f9b28ba5e87d82d247b0057d1f2ae103c35b4981bdeb3` ran through the final alpha68 executable and new `qwen3.8-27b-radiance-q4` ID in isolated real InferDeck. Only the ignored worker's live model-ID parameter changed; its fixture, xhigh reasoning, temperature/top-p/top-k/seed, 1024 output cap, exact tool/BUILD_TAG checks and cache criteria stayed fixed. Both cold requests had 64131 uncached prompt tokens, both made the expected tool call, and both continuations had 66173 prompt tokens with 63840 cached. All four requests returned HTTP 200, no output cap was hit, and the exact tasks passed. Complete two-agent batch time was **128.786 s** versus the matched BF16/R4D three-run median **133.684 s** and alpha67 Q4 136.871/137.930 s. Alpha68 generated 442 tokens versus 1183 in each BF16 baseline run, so the shorter task time does **not** establish faster prompt compute or decode; it is only the end-to-end result for the same validated task. The shared cold engine prefill spanned 94.344 s for both lanes, while warm uncached appends took 2.562/3.703 s. Evidence: `.UAM/AINotes/alpha68-q4-smoke-smoke-20260924T025417631Z.json`, `.UAM/AINotes/q4kv-64k2k-20260924T025521601Z-worker.json`, the staged `.gateway.stderr.log` and `data/stats.db`.

The runner reported `PASS_SCOPED_64K2K`, stopped the candidate, and restored production alpha55 with `qwen3.6-35b-a3b` loaded, zero active requests, no swap, and port 11435 closed. Read-only opt-in deployment preflight passed again after the package status update as `.UAM/AINotes/alpha68-q4-optin-20260924T025407961Z.json`. Production Q4 has not been activated. The 2K uncached PP target, four simultaneous complete 100K answers, matched process-dedicated VRAM parity, and 20 switch/100 mixed-request reliability gates remain unproven.

The independent read-only review found matching hashes and eight aligned deploy/rollback targets, with no concrete activation or rollback fault. It noted only that rollback may leave empty directories from newly created files. Final parser/goal JSON/diff checks and read-only deployment preflight passed; the latest preflight record is `.UAM/AINotes/alpha68-q4-optin-20260924T030113072Z.json`.

Next executable action: activate the reviewed opt-in package only after the already requested explicit production replacement authorization. Verify live alpha68 Q4 chat, existing BF16/Vulkan inventory, original-model restoration and rollback behavior; keep the broader unresolved gates open. If authorization has not arrived, leave production alpha55 serving and do not emit completion.

## 2026-09-24 process-dedicated VRAM comparison and load crash

The first guarded attempt to compare alpha68 Q4 and Vulkan idle memory failed before a sample: Q4 gateway PID 17140 crashed during model load. Windows Application event 86004 and `build/private-releases/alpha68-q4-smoke/logs/crash.log` identify `0xc0000005` in the external `tokenizers.pyd` 0.23.2 at offset `0x2ceaf1`. Event 85300 records the same offset during a BF16 Radiance startup the previous day, so this is a shared intermittent native tokenizer-load issue, not evidence specific to the Q4 kernel. The unchanged alpha68 Q4 stage had also passed earlier real-app runs. No usable full dump was retained. The first run's result is FAIL and remains preserved at `.UAM/AINotes/alpha68-process-vram-20260924T031355295Z.json`; a compact crash record is `.UAM/AINotes/alpha68-tokenizers-crash-20260924.json`. The guarded runner restored alpha55 and its original model.

One diagnostic retry set `TOKENIZERS_PARALLELISM=false` only in the isolated child processes; source, model artifacts, DLLs and the alpha68 executable stayed unchanged. Both real InferDeck candidates loaded in sequence on port 11435. Windows `GPU Process Memory(*)\Dedicated Usage`, filtered by each gateway PID and the same R9700 LUID, gave three stable idle samples per candidate: Q4 Radiance **27559.316 MiB**, Vulkan **26914.594 MiB**, Q4 **+644.722 MiB / +2.4%**. The alpha68 binary revision was `2332e133f82a28c3b016afa16cc919e89e378f0c`; stage executable and dependency hashes matched its manifest. The Vulkan config used IQ4_XS GGUF, Q4 KV, mmproj/MTP and four 100000-token slots (observed pool 400384); Q4 Radiance used Quark AWQ-MXFP4, INT4 KV, no vision and four 106496-token slots. This compares deployed-style idle process footprints, not only KV format, and does not measure inference peak or four concurrent long answers. Raw command/result/config/counter samples: `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .UAM/AINotes/Compare-Alpha68ProcessVram.ps1`, `.UAM/AINotes/alpha68-process-vram-20260924T031947264Z.json`. The retry completed `PASS_IDLE_MEMORY_MEASUREMENT_ONLY`, exited both candidates, and restored the original alpha55 executable with `qwen3.6-35b-a3b`, no swap/active requests and port 11435 closed. One successful retry does not prove that disabling tokenizer parallelism fixed the crash.

Current gate state: idle dedicated VRAM is close to Vulkan, but full memory parity under four long requests is IN_PROGRESS. Stability is FAIL because of the recurring native startup crash. The previous 64K two-agent task result remains valid for the unchanged alpha68 candidate, while production activation remains unauthorised and untested. Q4 is text-only; Vulkan retains the configured Qwen3.8 vision projector.

Next executable action: make one source-level, pre-import `TOKENIZERS_PARALLELISM=false` mitigation in the Radiance profile and add a narrow cold-load crash trace or dump capture. Run the focused Python tests, restage the changed profile with pinned hashes, then perform one guarded **real InferDeck** cold load with original-model recovery. If it fails again, use the captured stack to fix the actual path; if it passes, repeat enough cold loads to establish reliability before updating the opt-in package or requesting production replacement. Do not treat this single diagnostic retry as a stability PASS or rerun broad performance tests until the startup path is reliable.

## 2026-09-24 serialized-tokenizer diagnostic stage

The Radiance Python profile now sets `TOKENIZERS_PARALLELISM=false` before its vLLM/tokenizer imports and enables Python faulthandler at model creation. This is a bounded startup mitigation and diagnostic, **not a proven fix** for the intermittent native `tokenizers.pyd` access violation. The changed source profile SHA-256 was `f7922e1dad9f34fcb22098faeab262d71c660f46cf9a48b9fc1308b9157c76b2`. The focused Python suite passed 43/43 after setting the pinned profile/vLLM/site-package import paths; raw result: `.UAM/AINotes/alpha68-tokenizer-serial-python-tests.txt`. A read-only review found no confirmed functional bug in the two additions; it noted that faulthandler depends on usable `sys.stderr`, which the isolated real app had. The unchanged alpha68 gateway executable plus the changed profile and stage config were hash-checked in `build/private-releases/alpha68-tokenizer-serial/candidate-manifest.json`; this is a diagnostic stage, not the reviewed opt-in production package.

Guarded real InferDeck on port 11435 loaded Qwen3.8 Q4 once, answered a coherent greeting in 3.386 s, and measured 27559.316 MiB idle process-dedicated VRAM. Evidence: `.UAM/AINotes/alpha68-process-vram-20260924T033122990Z.json`. A second guarded load answered a coherent greeting in 3.421 s, passed four distinct simultaneous HTTP-200 short requests with four active slots, and passed the unchanged frozen two-agent 64K+2K tool fixture (SHA-256 `d4b52041f8e99d3e486f9b28ba5e87d82d247b0057d1f2ae103c35b4981bdeb3`). Both exact tasks passed, all four long-loop requests returned HTTP 200, each continuation had 63840 cached tokens, and 442 tokens were generated. Client batch time was **128.559 s**, compared with **128.786 s** on the previous alpha68 Q4 profile with the same 442 generated tokens. The 0.227 s difference is one-run noise, not an established speed gain. BF16/R4D median was 133.684 s with 1183 generated tokens, so that comparison does not isolate prompt or decode speed. Evidence: `.UAM/AINotes/alpha68-tokenizer-serial-smoke-20260924T033612811Z.json` and `.UAM/AINotes/q4kv-64k2k-20260924T033717616Z-worker.json`.

The runner stopped the candidate and restored production alpha55 with its original `qwen3.6-35b-a3b` resident, healthy API, zero active requests and no swap; port 11435 closed. No production executable/config replacement occurred. Two successful Q4 loads with serialized tokenizer workers are insufficient to clear the observed intermittent crash; stability remains FAIL. Four concurrent complete 100K answers, peak process-dedicated VRAM, 20 switch cycles/100 mixed requests and the 2K uncached PP target remain unverified. Q4 Radiance remains text-only; the Vulkan Qwen3.8 vision/mmproj path is unchanged.

For that exact 128.559 s two-agent loop, stage StatsDb rows 21-24 show both 64131-token cold prompts evaluated across the shared 94.297 s prefill span: **680.1 uncached PP per request, about 1360.2 aggregate PP** for the pair. The 2333-token uncached appends evaluated in 2.547/3.719 s, **916.0/627.3 PP**, with 63840 cached tokens each. Generation rates were 7.488/8.000 TPS on cold requests and 7.448/9.770 TPS on continuations. Gateway queue was below 0.14 ms for all four requests. This is the actual multi-agent prompt/decode profile behind the whole-loop time; it does not meet the original 2000 uncached PP target. Raw: `build/private-releases/alpha68-tokenizer-serial/data/stats.db` rows 21-24.

Next executable action: review and commit the tested profile/checkpoint, then use a bounded repeated cold-load series with full-dump capture if another `tokenizers.pyd` crash occurs. If startup proves reliable, refresh the pinned production opt-in package from this exact profile and run its read-only preflight before requesting executable/active-YAML activation. Keep production alpha55 serving until that gate is crossed.

The tested profile and ledger were preserved in local commit `2ef94a7` (`chore(radiance): bound tokenizer startup`). One additional guarded cold load of the same hash-checked Q4 stage passed as `PASS_Q4_COLD_LOAD_ONLY`; raw record `.UAM/AINotes/alpha68-process-vram-20260924T034509037Z.json`. This makes three successful Q4 cold loads of the changed profile across two guarded runner types. The runner exited the candidate and restored production alpha55 with `qwen3.6-35b-a3b` loaded, no swap and zero active requests. Stability remains FAIL because the earlier native tokenizer crash was intermittent and three successes do not establish a fix.

Next executable action: refresh the existing opt-in package with the committed profile and its hashes, run the deployment helper's **read-only** preflight, and leave production unchanged. Then decide whether a targeted cold-start crash trace/repetition is needed before seeking production activation; do not claim reliability from three clean starts.

The ignored opt-in package's embedded Python profile was refreshed from commit `2ef94a7`; its SHA-256 now matches tracked source and the real-app diagnostic stage at `f7922e1dad9f34fcb22098faeab262d71c660f46cf9a48b9fc1308b9157c76b2`. The executable build revision and all live baseline hashes stayed unchanged. The helper's no-switch preflight passed as `PREFLIGHT_OK alpha68-q4-optin-20260924T034940578Z` after verifying package hashes, NSSM target, live service/config/registry and idle state; evidence `.UAM/AINotes/alpha68-q4-optin-20260924T034940578Z.json`. An automatic approval review initially rejected the deployment-named script because it could not infer that no-argument mode was read-only. Inspection showed `if (-not $Execute) ... exit 0` before the first production write; the same no-switch command was then approved and passed. Production still runs alpha55 with `qwen3.6-35b-a3b` loaded. `goals/q4-kv/opt-in-deployment.md` now describes the exact refreshed package and open gates.

Next executable action: target the intermittent `tokenizers.pyd` cold-load fault with a bounded real-app stability check or capture a full crash dump if it recurs. Do not treat the three clean changed-profile starts or the package preflight as a stability PASS. Production activation remains a separate explicit authorization and should not occur while that gate is unresolved without a deliberate risk decision.

## 2026-09-24 final-profile 100K and tokenizer probes

The committed profile (`2ef94a7`, SHA-256 `f7922e1dad9f34fcb22098faeab262d71c660f46cf9a48b9fc1308b9157c76b2`) passed a frozen real-app 100K coding/tool case. The unchanged fixture SHA-256 was `94ae060e5209d2204e95b1856fb9e4029030957268842de798834b68b5e5fab6`, with xhigh reasoning and a 1024-token output cap. Cold input was 100417 tokens, the continuation was 104560 tokens with 100320 cached, the first response ended in `tool_calls`, and the second ended in `stop`. The exact shared validator returned zero; complete task time was **99.072 s**. Raw: `.UAM/AINotes/alpha68-tokenizer-serial-smoke-20260924T035240958Z.json`, its `.quality100k.result.json` and `.quality100k.validation.txt`. The runner restored alpha55 with its original model and closed port 11435.

Stage StatsDb rows 31-32 attribute that 99.072 s task: cold prompt evaluation 81.032 s for 100417 uncached tokens (**1239.2 PP**), followed by 81 generated tokens at 15.904 TPS; cached continuation evaluated 4240 uncached tokens in 4.625 s (**916.8 PP**) and generated 126 tokens at 15.811 TPS. Gateway queue was below 0.2 ms per request. The row request durations sum to 99.052 s, within 20 ms of client task time. PP uses uncached evaluated tokens divided by prompt-evaluation seconds; cached tokens are excluded. Raw database: `build/private-releases/alpha68-tokenizer-serial/data/stats.db`.

One guarded real-app four-request pressure run on the same profile admitted four simultaneous **100194-token cold inputs**, observed four active slots, returned four HTTP 200 responses, and finished the client batch in **365.345 s**. Outputs were 192, 186, 233 and 256 tokens; the fourth reached the fixture's explicit 256-token cap. This proves input capacity and admission, not four complete long-answer quality. The unchanged 106496-token per-request context left output headroom. Seven Windows per-PID dedicated-VRAM samples during active work were all **28704.840 MiB**; they are sparse samples, not an independently captured true peak, and no matched Vulkan four-100K process trace exists. Raw: `.UAM/AINotes/alpha68-tokenizer-serial-smoke-20260924T035955870Z.json`, its `.four-100k.json`, and `.UAM/AINotes/alpha68-four100k-process-vram-20260924.jsonl`. Production alpha55 and `qwen3.6-35b-a3b` restored with no swap, and port 11435 closed.

The four concurrent cold requests were materially slower per agent: StatsDb rows 38-41 recorded prompt evaluation 196.688/196.688/312.844/282.078 s, or **509.4/509.4/320.3/355.2 uncached PP per request**, with generation **1.217/1.247/5.189/3.188 TPS**. Gateway queue remained below 0.2 ms; the contention is within the engine/GPU path, not gateway admission. These are per-request rates during contention, not an aggregate 2K PP claim. Total client batch time remains the primary four-agent measure and has no same-fixture Vulkan comparator.

Source inspection places the native startup fault inside fast-tokenizer construction in vLLM renderer initialization, before InferDeck's pooled-tokenizer lock is installed. No concurrent tokenizer initialization was evidenced. Five fresh CPU-only pinned-venv `AutoTokenizer.from_pretrained` constructions and five through the actual `ImmutablePooledTokenizer.from_pretrained` wrapper all passed against the unchanged local model files, with `tokenizers==0.23.2` and `TOKENIZERS_PARALLELISM=false`. Raw: `.UAM/AINotes/alpha68-tokenizer-construction-probe-20260924.txt` and `.UAM/AINotes/alpha68-pooled-tokenizer-construction-probe-20260924.txt`. The simpler CPU probes do not reproduce full-engine memory or backend state, so they do not establish the native crash cause. The changed profile has now completed five guarded Q4 cold loads, including these two 100K runs, but the prior intermittent crash keeps stability FAIL.

Next executable action: decide the minimum reliability proof for an opt-in production trial. A bounded repeated full-engine cold-load/switch run is more informative than more CPU-only tokenizer loops; if the access violation recurs, retain a full native dump and Python faulthandler trace for symbolized diagnosis. Keep the reviewed package opt-in and production alpha55 unchanged until a deliberate activation decision. Four complete concurrent 100K answers, matched active Vulkan VRAM and the original 20-switch/100-mixed-request gates remain open.
