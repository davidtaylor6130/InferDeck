# Q4 KV progress

## 2026-09-23/24 checkpoint

Status: IN_PROGRESS. Current production alpha55 BF16/R4D is running; after a guarded diagnostic interruption and service restart, `qwen3.8-27b-radiance` was loaded and a real `/v1/chat/completions` request returned HTTP 200 with `OK` (14 generated tokens). No Q4 deployment.

The private decode overlay has synthetic GPU parity, but no real InferDeck request has exercised the new decode DLL. A later optional overlay probe passed parity with a malformed PowerShell context argument (`64,65536` became `6465536`), so it is **not** the intended 64K check. The user directed us to stop offline checks and test through the actual app. The probe and watchdog were stopped. The probe's restore code used an unsupported `Expect: 100-continue` request and GPU admission was 4 MB short after unloading; an authorised service restart restored production. Evidence: `.UAM/AINotes/int4-decode-window-20260923T234011229Z.json`, `.UAM/AINotes/restore-inferdeck-service-result.json`.

Tracked source changes are the opt-in INT4 decode DLL selection/contract and focused tests. Private overlay and HIP kernel are under `build/runtime-radiance-probe/r4d-feasibility/int4-fast/`. Candidate stage is `build/private-releases/alpha59-int4decode/`; its copied alpha58 manifest is not yet a test result. Branch `agent/runtime-pp-context-evidence` has five modified tracked files. No commit this checkpoint.

Alpha59 staging is complete: Release gateway built; Python profile tests 27/27 and focused C++ model/config suites 2/2 passed. The staged executable and four bridge modules match the built/source files; config and manifest pin the decode DLL SHA-256. Stage manifest remains `STAGED_NOT_REAL_TESTED` until a real app run. The isolated test recovery scripts now use the documented swap endpoint without an unsupported request body; PowerShell syntax validation passed.

Next exact action: run `.UAM/AINotes/int4fast-smoke.ps1 -StageName alpha59-int4decode -LongContext -ServiceStop` once through the staged InferDeck app. Check gateway logs, actual greeting, frozen two-agent worker, production restoration, and whole-loop comparison. If startup or greeting fails, fix that specific failure before repeating a long run.

## 2026-09-24 alpha59 real-app result

Alpha59 ran through the actual staged Windows InferDeck gateway on port 11435, with production service stopped and independently recoverable. Real Qwen3.8 27B loaded with INT4 KV, native `r4d_int4` prefill and decode DLL active, context 106496, and one coherent greeting in 2.691 s. Frozen two-agent 64K+2K tool loop passed both exact tasks, four HTTP 200 requests, and 63840 cached tokens per continuation. Complete client batch time was 200.737 s. The current BF16/R4D three-run median on that fixture is 133.684 s, so this Q4 candidate is 50.16% slower and fails the 147.052 s initial similar-speed screen. Alpha58 prefill-only INT4 took 184.150 s in one run, but output-token counts differ, so that difference alone does not isolate the decode kernel. Raw: `.UAM/AINotes/alpha59-int4decode-smoke-20260923T235103217Z.json` and `.UAM/AINotes/q4kv-64k2k-20260923T235155609Z-worker.json`. Stage manifest is `FAIL_TOTAL_TIME_DO_NOT_DEPLOY`.

Production alpha55 BF16/R4D was restored, with the original model loaded, the staged port closed, and a real chat reply `OK` / HTTP 200. Recovery took about two minutes because service startup loaded its configured default Ornith model before swapping to Qwen. No Q4 production deployment. The goal is not complete.

Exact alpha59 request statistics are in `build/private-releases/alpha58-int4fast/data/stats.db` rows 23-26 because the copied alpha59 YAML still pointed its log/state/store/stats paths into alpha58. The worker IDs identify those rows; do not confuse them with alpha58 rows 18-21. Alpha59 queue was 0/54.3/54.4/66.4 s, cold prompt evaluation 44.688/44.797 s at 1435/1432 PP, cached append evaluation 2.047/2.046 s for 2333 uncached tokens at ~1140 PP, and generation 7.57-7.90 TPS. Q4 decode is slower than alpha58's 9.76-10.07 TPS. The staged config paths are now isolated to alpha59 and parse as YAML; current config SHA-256 `edccfc981602800e3dfbd50837fe05524083f31f0169b89809b58e3f5e14f066`, which differs from the measured run. No new performance claim applies to the corrected config. The 30000 MB residency figure is an estimate; peak dedicated VRAM was not captured.

Next exact action: inspect the private Q4 decode HIP kernel and the measured 7.57-7.90 TPS path for one bounded speed change. Then rebuild/stage and rerun the same frozen real InferDeck fixture once. Retain production BF16 until Q4 passes the total-time gate.

## 2026-09-24 alpha64 four-slot checkpoint

Status: IN_PROGRESS. The goal tool now tracks the four-slot Q4 objective. Production `0.9.0-alpha-55` BF16/R4D was restored after the guarded stage and is serving its original `qwen3.6-35b-a3b` model. Do not describe Q4 as deployed.

Alpha61-63 four-request tests failed three of four requests with a vLLM `KeyError` at `_gather_mm_embeddings`. Their stage roots had an updated Python profile, but the gateway actually imported stale `stage/python/inferdeck_vllm_radiance_profile.py`; those runs did not test the new engine lock. Alpha64 copied and hash-checked the imported path against tracked source before interrupting production. Real Qwen3.8 Q4 loaded with 169 KV blocks, group-aware calculated maximum concurrency 4.0238 at context 106496, and native INT4 prefill/decode. Four simultaneous distinct short API requests returned HTTP 200 with four active slots at peak. Actual GPU telemetry after those requests was 28454 MiB dedicated VRAM. Evidence: `.UAM/AINotes/alpha64-int4loaded-smoke-20260924T005613404Z.json`, its `.four-slots.json`, staged stderr, and `build/private-releases/alpha64-int4loaded/candidate-manifest.json`.

The frozen two-agent 64K+2K tool loop produced four HTTP 200 responses; both exact BUILD_TAG tasks passed and each warm continuation reported 63840 cached tokens. Batch wall time was 151.471 s versus BF16/R4D three-run median 133.684 s, 13.3% slower and above the initial 147.052 s screen. The Q4 run generated 894 tokens total versus 1183 for the BF16 baseline, so output-length differences do not explain away the slowdown. Q4 cold prompt evaluation was ~45.985/46.062 s at 1395/1392 PP; warm append evaluation ~2.078/2.093 s at ~1122 PP. One Q4 cold request had 90.2 s visible TTFT despite ~46 s engine prompt duration. Gateway queue time was <0.2 ms per request; source timing does not yet distinguish vLLM scheduling delay from bridge/parser delivery. Raw: `.UAM/AINotes/q4kv-64k2k-20260924T005720082Z-worker.json` and `build/private-releases/alpha64-int4loaded/data/stats.db`.

The outer worker incorrectly marked the run FAIL because it required exactly 66173 warm prompt tokens. One valid tool call changed the warm prompt to 66188 tokens; the other remained 66173. Its exact answer/quality validator passed. The ignored worker now checks a 2000-4000-token uncached append, >=63000 cached tokens, and output headroom; `node --check` and synthetic fixture preflight passed. The raw alpha64 record is unchanged and remains marked FAIL; repeat real measurement is needed for a PASS under the corrected worker. The stage script's generic 'quality, cache, or fixture' failure text was overly broad. Production recovery was independently checked: health true, original model loaded, no active swap.

Current tracked edits are config/model contract tests, four-slot C++ lifecycle/GIL/output handling, Python INT4 profile/engine lock and tests, plus this goal record. The native HIP source/overlay and DLLs are still in ignored `build/`; a Luna agent is copying minimal source to tracked wrapper files without touching existing source. No commit yet. Image support is optional for Q4; the existing Vulkan vision route must remain functional.

Next executable action: inspect the source-copy result and preserve/build the exact Q4 overlay and HIP kernels, then make one measured change to improve the 151.471 s two-agent loop or document why BF16 remains the speed choice. Re-stage with imported-profile/overlay hash preflight, rerun the corrected real-app worker, and verify production recovery. After a competitive result, exercise four long ~100K requests and commit only tested functional source.

## 2026-09-24 source-layout retry checkpoint

Minimal INT4 HIP prefill/decode source and the exact overlay bytes from alpha64 were copied under `libs/vllm_radiance_wrapper/native/int4/`. Both HIP translation units compiled with ROCm 7.1 for gfx1201; Windows host DLLs expose the required prefill and decode C exports. The copied overlay has the same SHA-256 as alpha64. The pinned vLLM/Radiance/Python/ROCm dependencies remain external. `native/int4/README.md` records the commands and unresolved upstream source-license provenance; no push/publication is approved.

Alpha65 was the first stage built from those tracked source files. It failed during model load because the byte-identical overlay was placed under `python/`, while its own source-hash check resolves a sibling `kernel/` directory. The guarded runner stopped only the failed staged gateway and restored production alpha55 with its original `qwen3.6-35b-a3b` model. Evidence: `.UAM/AINotes/alpha65-int4tracked-smoke-20260924T010958279Z.json` and its staged stderr. This is a source-layout failure, not a model/kernel correctness result.

The overlay was moved without changing bytes to `native/int4/r4d-feasibility/int4-fast/overlay/`, beside the expected `kernel/`. Alpha66 has isolated data/config, reuses the freshly compiled source DLLs, and pins the actual imported profile, overlay, executable, YAML and DLL hashes. The stage preflight now checks the overlay's source-relative kernel paths before production interruption. A guarded alpha66 real-app run with four short requests and the corrected 64K+2K worker is in progress. The Python profile also records one concise Q4 engine queue/prefill/generation timing event per completed request to attribute the prior 44-second TTFT gap. This diagnostic passed 31 focused Python tests after a mock-state compatibility fix.

Next executable action: inspect alpha66 run record and stderr timing events, verify original-model recovery, then decide whether one bounded tuning change is justified. Do not treat alpha65's failed load or alpha64's outer worker FAIL as passing evidence. A separate four near-100K cold-request worker was prepared but has not been run.

## 2026-09-24 alpha66 measured result

Alpha66 source-rebuilt stage loaded real Qwen3.8 with INT4 KV, native prefill/decode DLLs, four 106496-token slots, and the relocated tracked overlay. Four distinct simultaneous short requests all returned HTTP 200 and peak active slots were four. The corrected frozen two-agent 64K+2K worker passed both exact tool tasks, all four HTTP requests, and 63840 cached tokens on each append. Complete batch time was 151.354 s, versus BF16/R4D median 133.684 s and the 147.052 s initial screen. It is 13.22% slower than BF16 in this fixture; do not deploy/promote for speed. Dedicated VRAM was 28454 MiB after four short requests and 29502 MiB after the long loop. Production alpha55 and its original `qwen3.6-35b-a3b` model restored; staged process exited. Evidence: `.UAM/AINotes/alpha66-int4layout-smoke-20260924T011354761Z.json`, `.four-slots.json`, `.gateway.stderr.log`, `.UAM/AINotes/q4kv-64k2k-20260924T011502948Z-worker.json`, stage `data/stats.db`, and `candidate-manifest.json`.

The new per-request engine timing records show 43.937 s internal vLLM queue before the second cold 64131-token request, 45.922/46.031 s prompt evaluation on the two cold requests, and a 57.265 s generation span for the first while it was interleaved with the second prefill. InferDeck gateway queue was effectively zero. Thus the earlier ~44-second TTFT gap is vLLM internal scheduling, not proven bridge starvation. Alpha64 and alpha66 wall times differed by only 0.117 s, with the same 894 total generated tokens; the copied source/build did not improve speed. All 148 C++ unit/integration CTest cases and 31 Python profile tests passed; the Windows HIP source built with required exports. No public push due unresolved source-license provenance.

Next executable action: make a local tested experimental checkpoint commit, then try `long_prefill_token_threshold=2048` for INT4 only while retaining batch 4096 and all model/context/sampling settings. Run the same real two-agent fixture once, record internal queue, total time, output lengths and quality, and keep/revert based on whole-loop time. If the candidate is competitive, run four actual near-100K requests; otherwise still test capacity on the best functional profile before claiming four-slot parity.
