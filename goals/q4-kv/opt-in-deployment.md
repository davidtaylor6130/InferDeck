# Qwen3.8 Radiance Q4 opt-in candidate

Status: **deployed as an explicit opt-in on the `0.9.0-alpha-68` Windows service**. The previously loaded `qwen3.5-4b` was restored after validation; the default model, aliases, BF16 Radiance and Vulkan/vision model remain available. Select `qwen3.8-27b-radiance-q4` by ID. This is a local experiment, not a public release or new default. The measured two-agent loop missed the BF16/R4D total-time comparison, and long-running stability remains open.

## Candidate and evidence

The deployed package is `build/private-releases/alpha68-q4-active-optin/`. Its manifest pins the executable, active-profile YAML, embedded Python profile, INT4 overlay, two required kernel source/header files, both native DLLs and static assets by SHA-256. The executable reports `0.9.0-alpha-68 revision=2332e133f82a28c3b016afa16cc919e89e378f0c dirty=false`; executable SHA-256 is `77c8bab84495e82d70ac672383c0723439df602984b190f703e9b1eb131befd1`. The active YAML SHA-256 is `4c60ebd70a2f089db6018baf06999cd2517698a236fcc3af96e7bc04513d6181`. The packaged Python profile includes local commit `b9b7708` and SHA-256 `b29d20315791ca5ec5f89a948589428171813ca58ff6c998fca00f4a86cc8436`; the executable build revision remains the earlier alpha68 revision because C++ did not change.

Configure with `cmake -S . -B build -DINFERDECK_LOCAL_BUILD_NUMBER=68`, then build `inferdeck-gateway` in Release. `--version` and the repository's C++ unit/integration suite passed on the committed executable source: 148/148 tests. The pre-hotfix engine-lock profile passed 43/43 focused tests, including the request-preparation lock assertion; `.UAM/AINotes/alpha68-engine-lock-python-tests-20260924.txt`. Its guarded real InferDeck run completed four concurrent 100194-token cold inputs with four active slots, HTTP 200, `finish_reason=stop`, and no 512-token output-cap hit in 378.109 s; `.UAM/AINotes/alpha68-tokenizer-serial-smoke-20260924T053324965Z.json` and its `.four-100k-complete.json`. This validates normal completion, not the content of every answer. Production alpha55 and its original model were restored afterward.

The pre-hotfix engine-lock profile's frozen two-agent 64K+2K tool loop passed both exact answers and reused 63840 cached tokens per continuation, but took **154.029 s** and generated **902 tokens**. That is 20.345 s (15.2%) slower than the BF16/R4D 133.684 s median for the complete task. The previous Q4 profile took 128.559 s with 442 generated tokens on the same frozen fixture; therefore that profile cannot inherit that faster total-time result. Cold engine prefill improved from 94.297 s to 87.687/88.406 s, while longer generated answers dominated actual elapsed time. Warm generation rates remained roughly 7.15/9.54 versus 7.45/9.77 tokens/s. One run cannot establish whether request scheduling changed output length. Raw pre-hotfix evidence is `.UAM/AINotes/alpha68-tokenizer-serial-smoke-20260924T054357345Z.json`, `.UAM/AINotes/q4kv-64k2k-20260924T054508110Z-worker.json` and matching candidate stderr. The older 128.559 s run remains in `.UAM/AINotes/q4kv-64k2k-20260924T033717616Z-worker.json`.

The prior Python profile passed the frozen single-agent 100K coding/tool case in 99.072 s: 100417 cold input tokens, 100320 cached on a 104560-token continuation, exact tool and final-answer checks, and no output-cap hit. Its earlier four simultaneous 100194-token cold inputs completed in 365.345 s, but one response reached the 256-token cap. Those results do not validate the later engine-lock profile's content or speed. Evidence: `.UAM/AINotes/alpha68-tokenizer-serial-smoke-20260924T035240958Z.json` and `.UAM/AINotes/alpha68-tokenizer-serial-smoke-20260924T035955870Z.json` with their worker outputs.

The previous profile's StatsDb recorded about 1360.2 aggregate uncached PP across two simultaneous 64K cold prompts; individual prompt rates were 680.1 PP. Its single 100K cold request reached 1239.2 PP and 15.9 generation TPS, then 916.8 PP on a 4240-token uncached append with 100320 reused. The pre-hotfix engine-lock profile's four concurrent cold 100K requests measured 317.4-499.2 PP per request with recorded queue time below 0.3 ms; `build/private-releases/alpha68-tokenizer-serial/data/stats.db` rows 177-180. Four-slot capacity is available, but fast four-agent throughput and the original 2000 uncached PP target are not established.

Alpha67's frozen two-agent 64K+2K tool-loop times were 137.930 and 136.871 seconds versus the BF16/R4D 133.684-second median. Its four simultaneous cold inputs each contained 100194 tokens and returned HTTP 200, but two answers reached the explicitly requested 256-token output cap. A separate 100K tool continuation completed normally with exact `read_file` and `BUILD_TAG` checks, 100320 cached tokens, and 99.074 seconds from first send to final answer. These results support an opt-in trial, not a claim of higher raw PP or complete long-term stability.

The earlier 29557.65625 MiB post-batch reading is GPU-wide VRAM used from `windows_pdh_dxgi`. A later Windows per-process dedicated counter measured idle Q4 Radiance at 27559.316 MiB and Vulkan Qwen3.8 at 26914.594 MiB on the same R9700, Q4 +644.722 MiB (+2.4%); raw result is `.UAM/AINotes/alpha68-process-vram-20260924T031947264Z.json`. The previous Python profile repeated the 27559.316 MiB Q4 idle result. Seven sparse per-process samples during four active 100194-token Q4 requests on that profile were all 28704.840 MiB; `.UAM/AINotes/alpha68-four100k-process-vram-20260924.jsonl`. Final-profile dedicated-VRAM peak was not sampled.

A guarded Vulkan run used the same source fixture, shortened 2600 characters per lane to leave output headroom under its 100000-token request limit. InferDeck tokenized and reserved 99831 context positions per request and admitted four concurrent slots. Seventy-one Vulkan process-dedicated samples during activity measured 27459.492-27459.496 MiB, about 1245.3 MiB (4.54%) below Q4's observed four-active reading. However, all four Vulkan requests returned no response by the 720-second client timeout, whereas Q4's four 100194-token requests completed in 365.345 seconds. Raw records are `.UAM/AINotes/alpha68-process-vram-20260924T050255854Z.json`, its `.vulkan-four-99k.json` and `.vulkan-active-memory.jsonl`. The timeout means this is an active footprint comparison and a lower bound on Vulkan batch latency, not a completed matched benchmark or true peak. Artifacts, context sizes, vision and MTP also differ. Dedicated-VRAM parity remains IN_PROGRESS pending a completed matched workload or operational peak.

## Configuration boundary

Production uses `C:\InferDeck\config\gateway.active.yml`, **not** the base `gateway.yml`. The package's `config/gateway.active.optin.yml` added one model before `model_aliases:`; YAML structural comparison confirmed that the other active settings were unchanged. Its new ID is `qwen3.8-27b-radiance-q4`, with four fixed 106496-token slots, `r4d_int4`, `int4_per_token_head` KV and text-only capability. Its separate `radiance-qwen3.8-q4` request-admission pool permits four slots while the existing BF16 profile retains its one-slot pool; both declare 30000 MB against the detected 32021 MB GPU budget. The default, aliases and Vulkan `qwen3.8-27b` with `mmproj_path`/vision remain unchanged. Select Q4 explicitly by its new ID.

The added registry entry was compared with the deployed active entry by a YAML parse. The tracked `goals/q4-kv/q4-optin-model.yml` preserves its settings and hashes with workstation paths replaced by `C:/path/to/InferDeck`. The full active YAML and binary package remain local under `build/private-releases/alpha68-q4-active-optin/` and `C:\InferDeck`.

The first package built from `gateway.yml` was invalidated as `INVALIDATED_BASE_CONFIG_NOT_ACTIVE`. Do not deploy `build/private-releases/alpha68-q4-optin/` or `alpha67-prefill2048/deployment/gateway.optin.yml`.

Run `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .UAM/AINotes/Deploy-Alpha68Q4OptIn.ps1 -Preflight` for a **read-only** check against the manifest's current alpha68 baseline. The hash-pinned hotfix package passed as `PREFLIGHT_OK alpha68-q4-optin-20260924T063442486Z`; raw evidence is `.UAM/AINotes/alpha68-q4-optin-20260924T063442486Z.json`. The helper requires an explicit mode and exits before backup, service stop or copy in `-Preflight`. Its separate `-Execute -RootReviewed` mode was used for the authorized activation.

The first successful live activation is `.UAM/AINotes/alpha68-q4-optin-20260924T062917248Z.json`. After it exposed the `none` reasoning mismatch, local commit `b9b7708` mapped `none` to disabled thinking in the Radiance profile. Its focused profile suite passed 31/31 tests and an independent diff review found no issue. The broader Python discovery run crashed in the unchanged native tokenizer-construction test, so it is not a passing suite. The guarded hotfix deployment `.UAM/AINotes/alpha68-q4-optin-20260924T063451647Z.json` passed real Q4 low- and no-reasoning chat checks; both finished with the requested answer. The no-reasoning request changed from HTTP 503 before the fix to HTTP 200/`finish_reason=stop` after it. The service now reports alpha68 healthy with the active profile, while `qwen3.5-4b` is loaded and Q4, BF16 Radiance and Vulkan Qwen3.8 are all registered. This validates activation and two short chat paths, not the outstanding long-context performance or repeated-load stability gates.

## Activation and rollback

Before activation, confirm the current live active/base config and executable hashes still match `manifest.json`, the `InferDeck` NSSM target is `C:\InferDeck\inferdeck-gateway.exe -c config\gateway.yml`, the active profile is in use, the service has no active/queued requests, and every package artifact matches its manifest. Back up the live executable, active config, Python profile and any files at the new overlay/kernel/DLL destinations under `C:\InferDeck\backups\` before stopping the service. Keep the base config untouched. The package's static assets and all 24 root DLLs matched production by SHA-256 when prepared; verify this again and leave them unchanged if they still match.

After stopping only `InferDeck`, copy the alpha68 executable, candidate active YAML, Python profile, INT4 overlay, two required kernel source/header files and two hash-pinned DLLs to their `C:\InferDeck` destinations. Start the service and verify the reported alpha68 revision, active profile, model inventory and a real Q4 chat request. Swap back to the pre-deployment model and confirm it serves. If any step fails, stop the service, restore the backed-up executable/active YAML/Python profile and all touched files, restart, and verify the original model. Retain the backup and raw validation record. Do not change existing aliases or switch the default to Q4.

The successful hotfix backup is `C:\InferDeck\backups\alpha68-q4-optin-20260924T063451647Z`; its `rollback-state.json` restores the previous alpha68 Python profile. The earlier successful initial-activation backup `C:\InferDeck\backups\alpha68-q4-optin-20260924T062917248Z\rollback-state.json` restores the original alpha55 executable and active config if the entire opt-in must be removed. Run `Rollback-Alpha68Q4OptIn.ps1 -StatePath <one of those exact rollback-state.json paths>` from an elevated PowerShell, then verify `/api/inferdeck/v1/health`, `/api/inferdeck/v1/config` and `/api/inferdeck/v1/swap/status`. The script validates the backup hashes and NSSM target before changing any live file.

The local deploy helper backs up the eight touched destinations and starts an independent hidden watchdog before stopping the service. Adversarial review found and we fixed a hash-check race, a watchdog deadline shorter than the longest validation path, a missed stop-timeout rollback, unbounded rollback paths, and a watchdog activation-before-start case. The helper checks live and backup hashes again immediately before activation, restricts rollback to the eight destinations, and uses parent-process monitoring plus an 1800-second ceiling. It also suppresses PowerShell's unsupported `Expect: 100-continue` header for control POSTs. Three guarded live failures each restored the prior service: a shared-pool limit mismatch, a 96-token xhigh smoke exhausted before final text, and Radiance rejecting `reasoning_effort: none`. Raw rollback records are `.UAM/AINotes/alpha68-q4-optin-20260924T061252376Z.json`, `.UAM/AINotes/alpha68-q4-optin-20260924T061938276Z.json` and `.UAM/AINotes/alpha68-q4-optin-20260924T062449261Z.json`.

The deployed profile still references the existing local Python/ROCm/vLLM/Radiance environment and model artifact under `build/runtime-radiance-probe`; those large dependencies are **not** bundled. Do not clear or move that tree while Radiance is configured. LocalSystem startup and the alpha68 service-switch path passed live. Upstream source authorship/licensing for the native INT4 copy remains unresolved, so do not publish this package or source as a release.

Windows Application events 85300 and 86004 recorded intermittent native `tokenizers.pyd` access violations during BF16 and Q4 cold loads. A later real InferDeck attempt with four long requests crashed in the same native tokenizer path despite its tokenizer-local lock; `.UAM/AINotes/alpha68-tokenizer-serial-smoke-20260924T052240731Z.gateway.stderr.log`. Four identical 100194-token prompts passed CPU-only tokenization. Commit `9e9462e` then extended the existing engine lock over request preparation, and the guarded four-agent retry plus the exact two-agent tool loop passed. This is a plausible race fix, not a proven native-crash root cause or repeated stability proof. The prior profile completed 20 Q4/Vulkan switch cycles and 120 mixed requests; its worker PASS and independent audit are `.UAM/AINotes/alpha68-tokenizer-serial-smoke-20260924T042330971Z.switch20.json` and its `.switch20-audit.json`. That older stress result is not final-profile evidence. Broad stability remains IN_PROGRESS. Keep Q4 opt-in and the proven BF16/Vulkan paths available. Q4 Radiance is text-only; Vulkan Qwen3.8 retains its configured vision projector and is the choice for image requests.
