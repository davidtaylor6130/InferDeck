{
  "name": "inferdeck",
  "version": "0.7.2",
  "private": true,
  "description": "Production-ready local AI gateway for single-GPU machines",
  "workspaces": [
    "apps/dashboard"
  ],
  "scripts": {
    "dev:dashboard": "pnpm --filter dashboard dev",
    "build:dashboard": "pnpm --filter dashboard build"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.15.0"
}
